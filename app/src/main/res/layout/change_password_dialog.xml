<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:contentDescription="update your password dialog"
    tools:context=".ChangePasswordDialogFragment">

    <RelativeLayout
        android:id="@+id/Relative1"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="@color/khly_ghame"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:text="Update your password"
            android:contentDescription="Update your password"
            android:textColor="@color/white"
            android:textSize="22sp" />
    </RelativeLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/Relative1"
        android:id="@+id/linear1"
        android:orientation="vertical">

        <AutoCompleteTextView
            android:id="@+id/oldPassword"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginBottom="10dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="15dp"
            android:layout_marginStart="15dp"
            android:background="@drawable/rounded_white"
            android:drawableLeft="@mipmap/ic_pass_icon"
            android:drawablePadding="5dp"
            android:textColor="@color/colorPrimary"
            android:textColorHint="@color/colorPrimary"
            android:contentDescription="enter your old password here"
            android:hint="Old Password"
            android:paddingStart="10dp"
            android:inputType="textPassword"
            android:textSize="14sp" />

        <AutoCompleteTextView
            android:id="@+id/newPassword"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginBottom="10dp"
            android:layout_marginEnd="15dp"
            android:textColor="@color/colorPrimary"
            android:textColorHint="@color/colorPrimary"
            android:layout_marginStart="15dp"
            android:contentDescription="enter your new password here"
            android:background="@drawable/rounded_white"
            android:drawableLeft="@mipmap/ic_pass_icon"
            android:drawablePadding="5dp"
            android:hint="New Password"
            android:inputType="textPassword"
            android:paddingStart="10dp"
            android:textSize="14sp" />

        <AutoCompleteTextView
            android:id="@+id/confirmPassword"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginBottom="15dp"
            android:layout_marginEnd="15dp"
            android:layout_marginStart="15dp"
            android:contentDescription="enter your new password here again to confirm it"
            android:background="@drawable/rounded_white"
            android:drawableLeft="@mipmap/ic_confirm_icon"
            android:drawablePadding="5dp"
            android:textColor="@color/colorPrimary"
            android:textColorHint="@color/colorPrimary"
            android:hint="Confirm Password"
            android:inputType="textPassword"
            android:paddingStart="10dp"
            android:textSize="14sp" />
    </LinearLayout>

    <Button
        android:id="@+id/update_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/linear1"
        android:layout_centerHorizontal="true"
        android:background="#353F74"
        android:text="update"
        android:contentDescription="update button. click to update your password"
        android:layout_marginBottom="20dp"
        android:textColor="@color/white" />
</RelativeLayout>
